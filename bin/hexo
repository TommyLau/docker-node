#!/bin/sh
# Please place this file in /usr/local/bin, and set the execution right.
docker run -ti --rm -v $(pwd):/hexo -v ~/.ssh:/root/.ssh -v ~/.gitconfig:/root/.gitconfig -p 4000:4000 tommylau/hexo hexo $@
